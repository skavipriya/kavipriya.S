#include <stdio.h>

int main(void) {
	int a,n,s,d,sum=0;
	printf("enter the number");
	scanf("%d",&a);
	a=n;
	while(a!=0)
	{
		s=s+10+d;
	}
	if(sum==n)
	{
		printf("palindrome");
	}
	else
	{
		printf("not a palindrome");
	}
	return 0;
}
